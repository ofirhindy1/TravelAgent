import type { CSSProperties } from 'react'

export const pageStyle: CSSProperties = {
  height: '100%',
  display: 'flex',
  flexDirection: 'row',
}

export const chatAreaStyle: CSSProperties = {
  flex: 1,
  display: 'flex',
  flexDirection: 'column',
  minWidth: 0,
}
